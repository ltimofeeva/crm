{
  "name": "psy-crm-backend",
  "version": "0.1.0",
  "description": "Прокси к Claude API. Держит API-ключ на сервере, а не в телефоне.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
