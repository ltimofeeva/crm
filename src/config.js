// Адрес сервера ИИ (прокси к Claude API).
//
// ВАЖНО: адрес захардкожен намеренно. В веб-сборке Expo значение из
// app.json → extra.backendUrl НЕ подхватывалось (Expo зашивал сюда
// localhost:8787), из-за чего приложение на телефоне уходило «в никуда».
// Поэтому адрес задаётся прямо здесь. Если сменить сервер — поменяйте эту
// строку и пересоберите (npm run build:web).
export const BACKEND_URL = "https://crm-ujn8.vercel.app";
