// Адрес сервера ИИ (прокси к Claude API).
//
// Пусто = «тот же сервер, что отдаёт приложение» (относительный путь
// /api/chat). Так и приложение, и ИИ живут на одном VPS: приложение
// обращается к /api/chat на своём же домене. Если понадобится вынести ИИ
// на отдельный адрес — впишите сюда полный URL (https://...).
export const BACKEND_URL = "";
