// Дизайн-токены приложения. Одно место для палитры, отступов, скруглений и
// теней — меняешь здесь, меняется во всём приложении.

// Палитра «спокойная синяя»: прохладный светлый фон, глубокий синий как
// основной цвет, тёплый янтарный как акцент. Синий читается как надёжность,
// а тёплый акцент не даёт интерфейсу стать холодным.
export const C = {
  bg: "#F2F5F8",        // фон приложения
  surface: "#FFFFFF",   // карточки и поля
  ink: "#1E2A38",       // основной текст
  inkSoft: "#5A6B7D",   // второстепенный текст
  inkFaint: "#7C8CA0",  // подписи разделов, плейсхолдеры
  primary: "#2B5578",   // основной цвет: кнопки, активная вкладка
  primarySoft: "#E3EBF2",// светлая заливка основного
  accent: "#CE8452",    // тёплый янтарный акцент — главные действия
  accentSoft: "#F8EBE0",// светлая заливка акцента
  line: "#E4EAF0",      // тонкие разделители
  lineSoft: "#EFF3F7",  // почти невидимые линии
  white: "#FFFFFF",
  danger: "#C0544C",    // удаление/ошибки
};

// Шкала отступов — кратно 4. Пользуйся ими вместо «магических» чисел.
export const S = { xs: 4, sm: 8, md: 12, lg: 16, xl: 24, xxl: 32 };

// Скругления углов.
export const R = { sm: 10, md: 14, lg: 18, pill: 999 };

// Мягкие тени (работают и в вебе через react-native-web). Лёгкая
// приподнятость вместо жёстких рамок — так интерфейс выглядит чище.
export const SHADOW = {
  card: {
    shadowColor: "#16202B",
    shadowOffset: { width: 0, height: 6 },
    shadowOpacity: 0.06,
    shadowRadius: 16,
    elevation: 2,
  },
  soft: {
    shadowColor: "#16202B",
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.05,
    shadowRadius: 8,
    elevation: 1,
  },
};

// В React Native нет Georgia на Android. Для реального проекта подключите шрифт
// через expo-font. Пока используем системную serif-подстановку.
export const SERIF = { fontFamily: undefined, fontWeight: "700" };
